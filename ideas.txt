var responder = {
  'render': function() {
  }
}

var templateFunction = function() {
  //specify template to fill (like a base), and functions or templates to use to fill it (like body, top, header, sidebar)
  //optionally extend another template
}

var Posts = function() {};
Posts.prototype = responder;

Posts.list = function(request) {
  this.render( "index.html", { 'var1': var1, 'var2': var2});

  //or

  this.render( templateFunction, { 'var1': var1, 'var2': var2});
};

Posts.show = function(request, id) {  // or should the second option be a dictionary of the url parameters
};

var urls = [
  '/': 'Posts.list',
  '/:id': 'Posts.show',
]
